# Hybrid Dota 2 ML Trainer Dependencies
# Python 3.8+
# Install with: pip install -r requirements.txt

# Core ML & Data Science
tensorflow>=2.11.0
torch>=1.13.0  # Alternative to TensorFlow
torchvision>=0.14.0
numpy>=1.21.0
pandas>=1.3.0
scikit-learn>=1.0.0

# API & Networking
requests>=2.28.0
urllib3>=1.26.0

# Utilities
python-dotenv>=0.20.0
tqdm>=4.64.0
logging>=0.5.1.2

# Visualization (optional)
matplotlib>=3.5.0
seaborn>=0.12.0

# Development
ipython>=8.0.0
jupyter>=1.0.0
ipython-genutils>=0.2.0

# Testing
pytest>=7.0.0
pytest-cov>=3.0.0

# GPU Optimization (NVIDIA)
# If using GPU, install CUDA toolkit and cuDNN separately
# Or use: pip install tensorflow[and-cuda]>=2.11.0

# OpenDota API Integration
# Requires valid API key from https://www.opendota.com/api-keys

# Notes for RTX 3060:
# - batch_size: 64 (optimal for 12GB VRAM)
# - mixed_precision: False (not needed for RTX 3060)
# - distributed_training: Single GPU mode
# - num_workers: 4 (for data loading)

# Time Estimates for i5-10400F + RTX 3060 + 32GB RAM:
# Phase 1 (50k matches): 1.5-2 hours
# Phase 2 (5k games): 12-15 hours
# Total: 14-17 hours
